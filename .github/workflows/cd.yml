name: CD

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed

jobs:
  local-deploy:
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' && (github.event.workflow_run.head_branch == 'main' || github.event.workflow_run.head_branch == 'master')
    runs-on:
      - self-hosted
      - local-deploy
    permissions:
      contents: read
    concurrency:
      group: local-deploy
      cancel-in-progress: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Create environment file from repository secrets
        shell: bash
        run: |
          if [ -z "${{ secrets.DJANGO_SECRET_KEY }}" ]; then
            echo "Repository secret DJANGO_SECRET_KEY is required for local deployment."
            exit 1
          fi

          {
            echo "DJANGO_SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }}"
            echo "DJANGO_DEBUG=${{ vars.DJANGO_DEBUG || 'False' }}"
            echo "DJANGO_ALLOWED_HOSTS=${{ vars.DJANGO_ALLOWED_HOSTS || '127.0.0.1,localhost,0.0.0.0' }}"
          } > .env

      - name: Deploy containers
        shell: bash
        run: |
          docker compose down
          docker compose up -d --build

      - name: Show running services
        shell: bash
        run: docker compose ps
