name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  pre-build-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests before container build
        run: python manage.py test restaurant

  docker-build-run-test:
    needs: pre-build-tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build containers
        run: docker compose build

      - name: Start web container
        run: docker compose up -d web

      - name: Run tests after container start
        run: docker compose exec -T web python manage.py test restaurant

      - name: Stop containers
        if: always()
        run: docker compose down